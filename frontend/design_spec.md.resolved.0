# 微信小程序前端设计规范 (Frontend Design Spec)

## 1. 页面结构 (Page Flow)

小程序主要包含 3 个核心页面：

1.  **首页 (Home)**: 引导页，展示功能介绍和“开始筛查”按钮。
2.  **拍照/上传页 (Camera)**: 核心交互页，引导用户拍摄标准角度。
3.  **报告页 (Report)**: 结果展示页，结构化显示 AI 分析结果和建议。

## 2. 页面详细逻辑

### 2.1 首页 (`pages/index`)
*   **UI 元素**:
    *   Banner: "AI 口腔健康管家"
    *   主按钮: "立即开始筛查" (调用 `wx.chooseMedia`)
    *   底部: 免责声明 ("本工具仅供筛查参考...")
*   **逻辑**:
    *   点击开始 -> 弹窗选择 "拍照" 或 "相册"。

### 2.2 拍照/上传页 (`pages/camera`)
*   **功能**: 引导用户拍摄清晰的口内照。
*   **关键交互 (Overlay Guide)**:
    *   在相机预览层上方覆盖 **SVG 线框图**（半透明）。
    *   提供 "张嘴"、"拉开嘴唇" 的图示引导。
*   **API**:
    *   `wx.uploadFile`: 将图片发送至后端 `/predict` 接口。
    *   **Loading**: 上传时显示 "AI 正在分析中(约1-2秒)..."。

### 2.3 报告页 (`pages/report`)
*   **接收数据**: 来自后端的 JSON 响应。
*   **UI 模块**:
    1.  **综合评分卡**:
        *   大号数字展示分数 (e.g., "85分")。
        *   颜色动态变化 (Green > 90, Yellow > 70, Red < 70)。
        *   结果摘要: "您的口腔状况良好/一般/需注意"。
    2.  **牙位图 (Visual Map)**:
        *   使用 Canvas 绘制标准牙列图。
        *   根据 `bbox` 数据，在对应牙位上标记红点/红框。
        *   *简化方案*: 直接展示原图，并在原图上叠加 SVG 框 (根据后端返回的 `bbox` 坐标绘制)。
    3.  **问题列表 (Issues)**:
        *   折叠面板 (Collapse) 展示详细问题。
        *   UI: [图标] 标题 (e.g., "发现深龋风险") | 严重等级标签。
        *   内容: 对应部位 + 行动建议。
    4.  **底部栏**:
        *   "保存报告" (生成海报)。
        *   "咨询医生" (跳转客服/小程序内链接)。

## 3. 数据接口定义

### 请求
`POST {API_URL}/predict`
*   Header: `Content-Type: multipart/form-data`
*   Body: `file=@image.jpg`

### 响应 (JSON)
```json
{
  "health_score": 85,
  "summary": "发现一些潜在问题...",
  "issues": [
    {
      "type": "caries_medium",
      "title": "中风险",
      "action": "建议近期就医...",
      "severity": "medium",
      "count": 1
    }
  ],
  "meta": { "inference_ms": 120 }
}
```

## 4. 样式规范 (WXSS)
*   **主色调 (Brand)**: `#00BFA5` (医疗绿/蓝绿色，专业且不冷漠)。
*   **警告色 (Warning)**: `#FF9800` (橙色)。
*   **危险色 (Danger)**: `#F44336` (红色)。
*   **字体**: 使用系统默认无衬线字体，字号 14px/16px/24px 阶梯。
